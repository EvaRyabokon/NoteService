@model NotesService.Models.Note
@{
    ViewBag.Title = Model.Title;
}
<h2>Заметка</h2>
<div>
    <dl class="dl-horizontal">
        <dt>Заголовок</dt>
        <dd>@Model.Title</dd>

        <dt>Текст заметки</dt>
        <dd>@Model.Content</dd>
    </dl>
</div>
